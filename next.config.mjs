/** @type {import('next').NextConfig} */
const nextConfig = {
    // Opciones de configuración de Next.js
    // Ejemplo de configuración para especificar una carpeta de salida personalizada
    // distDir: 'build', // Esto generará la carpeta 'build' en lugar de '.next'
  };
export default nextConfig;
